# 🧾 Análise Técnica – Versão 1 do Sistema Bancário

## 📌 Visão Geral

Esta é a **versão inicial** de um sistema bancário simples, implementado em Python e executado via terminal. A aplicação permite realizar operações básicas como:

- Depósito
- Saque (com controle de limites)
- Emissão de extrato
- Encerramento da sessão

O código foi desenvolvido com foco na prática de estruturas básicas da linguagem e na simulação de um fluxo bancário realista, respeitando limites operacionais.

---

## ⚙️ Funcionalidades Implementadas

| Função    | Descrição |
|-----------|-----------|
| `Depósito` | Permite inserir valores positivos na conta e registra a operação no extrato. |
| `Saque`    | Realiza saques com duas restrições: até 3 saques diários e limite de R$500 por saque. |
| `Extrato`  | Lista todas as operações realizadas e o saldo atual. |
| `Sair`     | Encerra a execução do sistema. |

---

## 🧠 Estruturas Utilizadas

- **Função `main()`**: centraliza toda a lógica da aplicação.
- **Loop `while True`**: mantém o sistema ativo até que o usuário decida encerrar.
- **Condicionais (`if/elif/else`)**: controlam o fluxo com base nas opções do menu.
- **Tratamento de exceções (`try/except`)**: garante robustez na conversão de entradas para `float`.
- **Listas**: utilizadas para armazenar o histórico de transações (`extrato`).
- **Constantes**: utilizadas para representar limites operacionais (`LIMITE_SAQUES`, `LIMITE_POR_SAQUE`).
- **Manipulação de string**: adaptada para facilitar entrada de valores com vírgula como separador decimal.

---

## ✅ Boas Práticas Aplicadas

- Código encapsulado dentro de `main()` com a proteção `if __name__ == "__main__"`.
- Validação robusta das entradas numéricas.
- Uso de constantes para facilitar ajustes futuros.
- Comentários descritivos auxiliando na legibilidade do código.
- Nomenclatura clara e sem ambiguidade.

---

## ⚠️ Pontos de Melhoria para Versões Futuras

1. **Modularização**:
   - Separar funcionalidades em funções distintas (`depositar()`, `sacar()`, `exibir_extrato()`), melhorando clareza e reutilização.

2. **Abstração com POO**:
   - Introdução de classes como `ContaBancaria` e `Transacao`, promovendo encapsulamento e coesão.

3. **Persistência de dados**:
   - Armazenar saldo e extrato em arquivos ou banco de dados (ex: `JSON`, `SQLite`), garantindo continuidade entre sessões.

4. **Interface de usuário**:
   - Adotar interface gráfica (ex: `Tkinter`, `PyQt`) ou versão web futuramente.

5. **Internacionalização e formatação de moeda**:
   - Uso da biblioteca `locale` para adaptar valores monetários ao formato brasileiro corretamente.

6. **Testes Automatizados**:
   - Implementação de testes unitários com `unittest` ou `pytest`.

7. **Melhor tratamento de erros**:
   - Mensagens mais específicas para diferentes falhas de entrada (ex: campo vazio, caracteres inválidos).

---

## 📈 Conclusão

A **versão 1** cumpre com êxito os objetivos iniciais de criar uma simulação básica de sistema bancário. A aplicação está funcional, legível e organizada, servindo como alicerce para evoluções futuras mais robustas e orientadas a boas práticas de engenharia de software.

